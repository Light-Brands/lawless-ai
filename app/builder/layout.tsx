import '@/app/styles/builder.css';

export const metadata = {
  title: 'Builder - Lawless AI',
  description: 'Create project plans and brand identities with AI assistance',
};

export default function BuilderLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
